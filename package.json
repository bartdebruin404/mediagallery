{
  "name": " ",
  "version": "0.0.1",
  "description": "",
  "keywords": "",
  "license": "",
  "author": "",
  "scripts": {
    "build": "gulp sass && gulp img && parcel build index.html",
    "dev": "gulp sass && && gulp img parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.1",
    "gulp-sass": "^4.1.0",
    "node-sass": "^4.14.1",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.8"
  }
}
